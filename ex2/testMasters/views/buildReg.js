editting(localStorage.getItem('addres'));
function editting (reqName){
    if (reqName = ''){
        document.getElementsByClassName('varaity')[0].setAttribute(action, reqName);
    }    
    else{
        console.error('failed transition')
    }
    if(reqName = ''){
        document.getElementsByClassName('varaity')[0].setAttribute(action, reqName);
    }
    else{
        console.error('failed transition')
    }   
}